# 餐饮管理系统关键代码实现

## 一、用户点餐管理模块

### 1.1 订单创建接口（OrderController）

```java
@RestController
@RequestMapping("/api/orders")
@CrossOrigin(origins = "*")
public class OrderController {
    /**
     * 接口
     * POST /api/orders:创建订单
     * GET /api/orders/active:查询活跃订单
     * GET /api/orders:查询所有订单
     * GET /api/orders/ready:查询待结账订单
     * PUT /api/orders/{id}/status:更新订单状态
     * POST /api/orders/{id}/pay:订单支付
     */
    @Autowired
    private OrderService orderService;
    
    @PostMapping
    public ApiResponse<OrderVO> createOrder(@Valid @RequestBody CreateOrderRequest request) {
        OrderVO order = orderService.createOrder(request);
        return ApiResponse.success("订单创建成功", order);
    }
    
    @GetMapping("/ready")
    public ApiResponse<List<OrderVO>> getReadyOrders() {
        List<OrderVO> orders = orderService.getReadyOrders();
        return ApiResponse.success(orders);
    }
}
```

### 1.2 订单创建业务逻辑（OrderService）

```java
@Service
public class OrderService {
    /**
     * 创建订单（带事务控制）
     * 处理会员折扣、库存扣减、订单项生成
     */
    @Transactional
    public OrderVO createOrder(CreateOrderRequest request) {
        // 验证桌号有效性
        Table table = tableMapper.findByTableNumber(request.getTableNumber());
        if (table == null) {
            throw new RuntimeException("桌号不存在");
        }
        
        // 处理会员折扣
        BigDecimal discount = BigDecimal.ONE;
        Member member = null;
        if (request.getMemberPhone() != null) {
            member = memberMapper.findByPhone(request.getMemberPhone());
            if (member != null) {
                discount = member.getDiscount();
            }
        }
        
        // 生成订单并扣减库存
        String orderNumber = generateOrderNumber();
        Order order = new Order();
        order.setOrderNumber(orderNumber);
        order.setTableNumber(request.getTableNumber());
        // ... 设置其他字段
        
        orderMapper.insert(order);
        
        // 创建订单项
        for (OrderItemDTO itemDTO : request.getItems()) {
            Dish dish = dishMapper.selectById(itemDTO.getDishId());
            // 扣减库存
            dishService.updateStock(dish.getId(), -itemDTO.getQuantity());
            
            OrderItem item = new OrderItem();
            item.setOrderId(order.getId());
            item.setDishId(dish.getId());
            item.setQuantity(itemDTO.getQuantity());
            item.setPrice(dish.getPrice());
            orderItemMapper.insert(item);
        }
        
        return convertToVO(order);
    }
}
```

---

## 二、后厨管理模块

### 2.1 订单状态更新接口（OrderController）

```java
@RestController
@RequestMapping("/api/orders")
public class OrderController {
    /**
     * 更新订单状态
     * 状态流转：PENDING -> PREPARING -> READY -> COMPLETED
     */
    @PutMapping("/{id}/status")
    public ApiResponse<OrderVO> updateOrderStatus(
            @PathVariable Long id, 
            @Valid @RequestBody OrderStatusUpdateDTO dto) {
        OrderVO order = orderService.updateOrderStatus(id, dto);
        return ApiResponse.success("订单状态更新成功", order);
    }
}
```

### 2.2 WebSocket实时推送（WebSocketService）

```java
@Service
public class WebSocketService {
    /**
     * 向后厨推送订单更新消息
     * 使用STOMP协议订阅/topic/kitchen/orders主题
     */
    @Autowired
    private SimpMessagingTemplate messagingTemplate;
    
    public void notifyKitchenOrderUpdate(OrderVO order) {
        messagingTemplate.convertAndSend(
            "/topic/kitchen/orders", 
            ApiResponse.success("订单更新", order)
        );
    }
    
    public void notifyCashierOrderUpdate(OrderVO order) {
        messagingTemplate.convertAndSend(
            "/topic/cashier/orders", 
            ApiResponse.success("订单更新", order)
        );
    }
}
```

### 2.3 前端WebSocket订阅（kitchen/Index.vue）

```typescript
// 连接WebSocket并订阅订单更新主题
const connectWebSocket = () => {
  const socket = new SockJS('/api/ws')
  const stompClient = Stomp.over(socket)
  
  stompClient.connect({}, () => {
    // 订阅后厨订单主题
    stompClient.subscribe('/topic/kitchen/orders', (message) => {
      const data = JSON.parse(message.body)
      if (data.code === 200) {
        ElMessage.success('收到新订单通知')
        loadOrders() // 重新加载订单列表
      }
    })
  })
}

onMounted(() => {
  connectWebSocket()
  loadOrders()
})
```

---

## 三、收银管理模块

### 3.1 订单支付接口（OrderController）

```java
@RestController
@RequestMapping("/api/orders")
public class OrderController {
    /**
     * 订单支付
     * 支持现金、支付宝、微信支付
     * 更新订单状态为PAID，记录收银员和支付方式
     */
    @PostMapping("/{id}/pay")
    public ApiResponse<OrderVO> payOrder(
            @PathVariable Long id, 
            @Valid @RequestBody PaymentDTO paymentDTO) {
        OrderVO order = orderService.payOrder(id, paymentDTO);
        return ApiResponse.success("支付成功", order);
    }
}
```

### 3.2 支付业务逻辑（OrderService）

```java
@Service
public class OrderService {
    /**
     * 处理订单支付（带事务控制）
     * 更新订单状态、记录支付方式、更新会员积分
     */
    @Transactional
    public OrderVO payOrder(Long orderId, PaymentDTO paymentDTO) {
        Order order = orderMapper.selectById(orderId);
        if (order == null) {
            throw new RuntimeException("订单不存在");
        }
        
        // 更新订单状态
        order.setStatus("PAID");
        order.setPaymentMethod(paymentDTO.getPaymentMethod());
        order.setCashierId(paymentDTO.getCashierId());
        order.setPayTime(LocalDateTime.now());
        orderMapper.updateById(order);
        
        // 更新会员积分
        if (order.getMemberId() != null) {
            Member member = memberMapper.selectById(order.getMemberId());
            if (member != null) {
                int points = order.getActualAmount().intValue();
                member.setPoints(member.getPoints() + points);
                member.setTotalConsume(
                    member.getTotalConsume().add(order.getActualAmount())
                );
                memberMapper.updateById(member);
            }
        }
        
        // 更新桌号状态为空闲
        Table table = tableMapper.findByTableNumber(order.getTableNumber());
        if (table != null) {
            table.setStatus("EMPTY");
            tableMapper.updateById(table);
        }
        
        // 通过WebSocket通知收银台
        webSocketService.notifyCashierOrderUpdate(convertToVO(order));
        
        return convertToVO(order);
    }
}
```

---

## 四、业务管理模块

### 4.1 菜品管理控制器（DishController）

```java
@RestController
@RequestMapping("/api/dishes")
@CrossOrigin(origins = "*")
public class DishController {
    /**
     * 接口
     * GET /api/dishes:查询可用菜品
     * GET /api/dishes/all:查询所有菜品
     * GET /api/dishes/{id}:查询单个菜品
     * GET /api/dishes/category/{categoryId}:按分类查询
     * GET /api/dishes/recommend:查询推荐菜品
     * POST /api/dishes:新增菜品
     * PUT /api/dishes/{id}:修改菜品
     * PUT /api/dishes/{id}/status:更新上下架状态
     * DELETE /api/dishes/{id}:删除菜品
     */
    @Autowired
    private DishService dishService;
    
    @GetMapping
    public ApiResponse<List<DishVO>> getAvailableDishes() {
        List<DishVO> dishes = dishService.getAvailableDishes();
        return ApiResponse.success(dishes);
    }
    
    @PostMapping
    public ApiResponse<DishVO> createDish(@Valid @RequestBody DishDTO dishDTO) {
        DishVO savedDish = dishService.saveDish(dishDTO);
        return ApiResponse.success("菜品创建成功", savedDish);
    }
    
    @PutMapping("/{id}")
    public ApiResponse<DishVO> updateDish(
            @PathVariable Long id, 
            @Valid @RequestBody DishDTO dishDTO) {
        dishDTO.setId(id);
        DishVO updatedDish = dishService.saveDish(dishDTO);
        return ApiResponse.success("菜品更新成功", updatedDish);
    }
    
    @PutMapping("/{id}/status")
    public ApiResponse<DishVO> updateDishStatus(
            @PathVariable Long id, 
            @RequestParam Integer isAvailable) {
        DishVO dish = dishService.getDishById(id);
        DishDTO dishDTO = new DishDTO();
        dishDTO.setId(id);
        dishDTO.setIsAvailable(isAvailable);
        DishVO updatedDish = dishService.saveDish(dishDTO);
        return ApiResponse.success("菜品状态更新成功", updatedDish);
    }
    
    @DeleteMapping("/{id}")
    public ApiResponse<Void> deleteDish(@PathVariable Long id) {
        dishService.deleteDish(id);
        return ApiResponse.success("菜品删除成功", null);
    }
}
```

### 4.2 套餐管理控制器（ComboController）

```java
@RestController
@RequestMapping("/api/combos")
@CrossOrigin(origins = "*")
public class ComboController {
    /**
     * 接口
     * GET /api/combos:查询所有套餐
     * GET /api/combos/{id}:查询套餐详情（含菜品）
     * POST /api/combos:创建套餐及菜品关联
     */
    @Autowired
    private ComboService comboService;
    
    @GetMapping
    public ApiResponse<List<Combo>> getAllCombos() {
        List<Combo> combos = comboService.getAvailableCombos();
        return ApiResponse.success(combos);
    }
    
    @GetMapping("/{id}")
    public ApiResponse<ComboVO> getComboWithDishes(@PathVariable Long id) {
        ComboVO combo = comboService.getComboWithDishes(id);
        return ApiResponse.success(combo);
    }
    
    @PostMapping
    public ApiResponse<ComboVO> createComboWithRelation(
            @RequestBody ComboAndDishesRequest request) {
        Combo combo = request.getCombo();
        ComboDishConfigDTO dishesConfig = request.getDishesConfig();
        
        // 先保存套餐
        Combo savedCombo = comboService.saveCombo(combo);
        
        // 再配置菜品关联关系
        if (dishesConfig != null && dishesConfig.getDishes() != null) {
            dishesConfig.setComboId(savedCombo.getId());
            comboService.configureComboDishes(dishesConfig);
        }
        
        ComboVO comboVO = comboService.getComboWithDishes(savedCombo.getId());
        return ApiResponse.success("套餐及菜品配置成功", comboVO);
    }
}
```

### 4.3 套餐创建事务逻辑（ComboService）

```java
@Service
public class ComboService {
    /**
     * 配置套餐与菜品的关联关系（带事务控制）
     * 确保套餐和菜品关联要么全部成功，要么全部失败
     */
    @Transactional
    public void configureComboDishes(ComboDishConfigDTO config) {
        Long comboId = config.getComboId();
        
        for (ComboDishConfigDTO.DishItem dishItem : config.getDishes()) {
            ComboDish comboDish = new ComboDish();
            comboDish.setComboId(comboId);
            comboDish.setDishId(dishItem.getDishId());
            comboDish.setQuantity(dishItem.getQuantity());
            comboDishMapper.insert(comboDish);
        }
    }
}
```

### 4.4 会员管理控制器（MemberController）

```java
@RestController
@RequestMapping("/api/members")
@CrossOrigin(origins = "*")
public class MemberController {
    /**
     * 接口
     * GET /api/members:查询会员列表
     * GET /api/members/phone/{phone}:按手机号查询
     * POST /api/members:新增会员
     * PUT /api/members/{id}:修改会员信息
     */
    @Autowired
    private MemberService memberService;
    
    @GetMapping("/phone/{phone}")
    public ApiResponse<Member> getMemberByPhone(@PathVariable String phone) {
        Optional<Member> member = memberService.getMemberByPhone(phone);
        return member.isPresent() 
            ? ApiResponse.success(member.get()) 
            : ApiResponse.error(404, "会员不存在");
    }
    
    @PostMapping
    public ApiResponse<Member> createMember(@RequestBody Member member) {
        // 验证必填字段
        if (member.getPhone() == null || member.getPhone().trim().isEmpty()) {
            return ApiResponse.error("手机号不能为空");
        }
        
        // 设置默认值
        if (member.getLevel() == null) {
            member.setLevel("NORMAL");
        }
        if (member.getDiscount() == null) {
            member.setDiscount(new BigDecimal("1.00"));
        }
        
        Member createdMember = memberService.createMember(
            member.getPhone(), 
            member.getName()
        );
        return ApiResponse.success("会员创建成功", createdMember);
    }
    
    @PutMapping("/{id}")
    public ApiResponse<Member> updateMember(
            @PathVariable Long id,
            @RequestParam(required = false) String name,
            @RequestParam(required = false) String level,
            @RequestParam(required = false) BigDecimal discount) {
        Optional<Member> memberOpt = memberService.getMemberById(id);
        if (memberOpt.isPresent()) {
            Member member = memberOpt.get();
            if (name != null) member.setName(name);
            if (level != null) member.setLevel(level);
            if (discount != null) member.setDiscount(discount);
            Member updatedMember = memberService.updateMember(member);
            return ApiResponse.success("会员信息更新成功", updatedMember);
        } else {
            return ApiResponse.error(404, "会员不存在");
        }
    }
}
```

### 4.5 桌号管理控制器（TableController）

```java
@RestController
@RequestMapping("/api/tables")
@CrossOrigin(origins = "*")
public class TableController {
    /**
     * 接口
     * GET /api/tables:查询所有桌号
     * GET /api/tables/{tableNumber}:查询指定桌号
     * GET /api/tables/{tableNumber}/validate:验证桌号有效性
     */
    @Autowired
    private TableMapper tableMapper;
    
    @GetMapping
    public ApiResponse<List<Table>> getAllTables() {
        List<Table> tables = tableMapper.selectList(null);
        return ApiResponse.success(tables);
    }
    
    @GetMapping("/{tableNumber}")
    public ApiResponse<Table> getTableByNumber(@PathVariable String tableNumber) {
        Table table = tableMapper.findByTableNumber(tableNumber);
        if (table != null) {
            return ApiResponse.success(table);
        } else {
            return ApiResponse.error(404, "桌号不存在");
        }
    }
    
    @GetMapping("/{tableNumber}/validate")
    public ApiResponse<Boolean> validateTableNumber(@PathVariable String tableNumber) {
        Table table = tableMapper.findByTableNumber(tableNumber);
        boolean exists = table != null;
        return ApiResponse.success(exists);
    }
}
```

---

## 五、系统管理模块

### 5.1 用户管理控制器（UserController）

```java
@RestController
@RequestMapping("/api/users")
@CrossOrigin(origins = "*")
public class UserController {
    /**
     * 接口
     * GET /api/users:查询用户列表
     * GET /api/users/{id}:查询单个用户
     * POST /api/users:新增用户
     * PUT /api/users/{id}:修改用户
     * DELETE /api/users/{id}:删除用户
     * POST /api/users/{id}/reset-password:重置密码
     */
    @Autowired
    private UserService userService;
    
    @GetMapping
    public ApiResponse<List<UserVO>> getAllUsers() {
        List<UserVO> users = userService.getAllUsers();
        return ApiResponse.success(users);
    }
    
    @PostMapping
    public ApiResponse<User> createUser(@RequestBody User user) {
        // 验证角色权限：普通管理员只能创建收银员和后厨
        if ("ADMIN".equals(user.getRole()) && 
            !"admin".equals(user.getUsername())) {
            return ApiResponse.error("权限不足，无法创建管理员账号");
        }
        User savedUser = userService.saveUser(user);
        return ApiResponse.success("用户创建成功", savedUser);
    }
    
    @PutMapping("/{id}")
    public ApiResponse<User> updateUser(
            @PathVariable Long id, 
            @RequestBody User user) {
        user.setId(id);
        User updatedUser = userService.updateUser(user);
        return ApiResponse.success("用户更新成功", updatedUser);
    }
    
    @DeleteMapping("/{id}")
    public ApiResponse<Void> deleteUser(@PathVariable Long id) {
        // 验证权限：不能删除管理员账号
        if (id.equals(1L)) {
            return ApiResponse.error("权限不足，无法删除管理员账号");
        }
        userService.deleteUser(id);
        return ApiResponse.success("用户删除成功", null);
    }
    
    @PostMapping("/{id}/reset-password")
    public ApiResponse<Void> resetPassword(
            @PathVariable Long id, 
            @RequestBody Map<String, String> body) {
        String newPassword = body.get("newPassword");
        if (newPassword == null || newPassword.isEmpty()) {
            return ApiResponse.error("新密码不能为空");
        }
        userService.resetPassword(id, newPassword);
        return ApiResponse.success("密码重置成功", null);
    }
}
```

### 5.2 用户密码加密（UserService）

```java
@Service
public class UserService {
    @Autowired
    private PasswordEncoder passwordEncoder;
    
    /**
     * 保存用户（密码自动加密）
     */
    public User saveUser(User user) {
        // 使用BCrypt加密密码
        if (user.getPassword() != null && !user.getPassword().isEmpty()) {
            user.setPassword(passwordEncoder.encode(user.getPassword()));
        }
        userMapper.insert(user);
        return user;
    }
    
    /**
     * 重置密码
     */
    public void resetPassword(Long id, String newPassword) {
        User user = userMapper.selectById(id);
        if (user != null) {
            user.setPassword(passwordEncoder.encode(newPassword));
            userMapper.updateById(user);
        }
    }
}
```

### 5.3 数据统计控制器（StatisticsController）

```java
@RestController
@RequestMapping("/api/statistics")
@CrossOrigin(origins = "*")
public class StatisticsController {
    /**
     * 接口
     * GET /api/statistics/daily:查询每日统计数据
     * GET /api/statistics/stock-alerts:查询库存预警
     */
    @Autowired
    private DailyStatisticsMapper dailyStatisticsMapper;
    
    @Autowired
    private StockAlertMapper stockAlertMapper;
    
    @GetMapping("/daily")
    public ApiResponse<List<DailyStatistics>> getDailyStatistics(
            @RequestParam(required = false) LocalDate startDate,
            @RequestParam(required = false) LocalDate endDate) {
        // 默认查询近7天数据
        if (startDate == null) {
            startDate = LocalDate.now().minusDays(7);
        }
        if (endDate == null) {
            endDate = LocalDate.now();
        }
        
        List<DailyStatistics> statistics = dailyStatisticsMapper
            .findByStatDateBetweenOrderByStatDateDesc(startDate, endDate);
        return ApiResponse.success(statistics);
    }
    
    @GetMapping("/stock-alerts")
    public ApiResponse<List<StockAlert>> getStockAlerts() {
        // 查询所有未处理的库存预警
        List<StockAlert> alerts = stockAlertMapper
            .findByStatusOrderByCreateTimeDesc("ALERT");
        return ApiResponse.success(alerts);
    }
}
```

---

## 六、核心技术实现

### 6.1 统一响应封装（ApiResponse）

```java
@Data
public class ApiResponse<T> {
    private Integer code;      // 状态码：200成功，400错误，401未授权，404不存在
    private String message;    // 提示信息
    private T data;           // 响应数据
    
    public static <T> ApiResponse<T> success(T data) {
        return new ApiResponse<>(200, "操作成功", data);
    }
    
    public static <T> ApiResponse<T> success(String message, T data) {
        return new ApiResponse<>(200, message, data);
    }
    
    public static <T> ApiResponse<T> error(String message) {
        return new ApiResponse<>(400, message, null);
    }
    
    public static <T> ApiResponse<T> error(Integer code, String message) {
        return new ApiResponse<>(code, message, null);
    }
}
```

### 6.2 跨域配置（CorsConfig）

```java
@Configuration
public class CorsConfig {
    @Bean
    public CorsFilter corsFilter() {
        UrlBasedCorsConfigurationSource source = 
            new UrlBasedCorsConfigurationSource();
        CorsConfiguration config = new CorsConfiguration();
        
        // 允许所有来源（生产环境应配置具体域名）
        config.addAllowedOriginPattern("*");
        config.addAllowedHeader("*");
        config.addAllowedMethod("*"); // 允许所有HTTP方法
        config.setAllowCredentials(true);
        
        source.registerCorsConfiguration("/**", config);
        return new CorsFilter(source);
    }
}
```

### 6.3 安全配置（SecurityConfig）

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
    
    /**
     * 忽略Spring Security过滤的请求路径
     * 使/api/**路径的所有HTTP方法都能正常访问
     */
    @Bean
    public WebSecurityCustomizer webSecurityCustomizer() {
        return (web) -> web.ignoring().requestMatchers("/api/**");
    }
    
    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) 
            throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .authorizeHttpRequests(auth -> auth
                .anyRequest().permitAll()
            )
            .sessionManagement(session -> session
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)
            );
        return http.build();
    }
}
```

---

以上代码展示了餐饮管理系统五大核心模块的关键实现，涵盖了RESTful API设计、事务控制、WebSocket实时通信、密码加密、权限控制等核心技术点，为系统的稳定运行提供了可靠保障。
